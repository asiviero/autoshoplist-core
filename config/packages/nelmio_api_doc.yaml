nelmio_api_doc:
    documentation:
        info:
            title: Autoshoplist-core
            description: Base API and core functions of Autoshoplist
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
            # - ^/api(?!/ingredient$) # Accepts routes under /api except /api/doc
            #- api/unit/
            #- api/ingredient/
